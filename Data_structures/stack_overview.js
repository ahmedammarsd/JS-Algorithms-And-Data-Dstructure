/*
   - The stack data structure is swquential collection of elements that follows
     the principle of Last In First Out (LIFO)
   - The last element inserted into the stack is first element to be removed .

   == Support two main operations
    # Push , which adds an element to collection.
    # Pop, which removes the most recently added elemnt from the collection

   == Stack Usage
    # Browswe history tracking
    # Undo operation when typing 
    # Expression conversions
    # Call stack in JavaScript runtime 
*/
